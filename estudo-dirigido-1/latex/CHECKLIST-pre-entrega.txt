Checklist — antes de finalizar o relatório (ED1 / Lights Out)
===============================================================

Ambiente LaTeX (Windows) — se build.ps1 disser que pdflatex nao foi encontrado
[X] Instalar MiKTeX (https://miktex.org/download) OU TeX Live (https://www.tug.org/texlive/windows.html).
[X] Garantir que a pasta do binario esta no PATH (ex.: ...\MiKTeX\miktex\bin\x64 ou ...\texlive\AAAA\bin\win32).
[X] Fechar e reabrir o terminal/Cursor apos mudar o PATH.
[X] Testar no PowerShell:  Get-Command pdflatex
[X] Rodar:  powershell -ExecutionPolicy Bypass -File .\build.ps1  (a partir da pasta latex/)
[X] Se ainda falhar: o build.ps1 tambem procura MiKTeX/TeX Live em caminhos comuns; confira se pdflatex.exe existe nessas pastas.

Metadados e identificação
[X] Título reflete o trabalho (Lights Out / algoritmos de busca), NÃO o exemplo do template (bolo).
[X] Substituir placeholders de autor no main.tex (ex.: "SEU NOME AQUI", e-mail).
[X] Autor, curso, instituição (UTP) e e-mail institucional corretos.
[X] Ano (\date) conferido.

Resumo e conteúdo mínimo (enunciado)
[X] Resumo (abstract) descreve problema, objetivo e métodos em 1 parágrafo coerente.
[X] Introdução ao problema Lights Out (regras, objetivo, estado inicial).
[X] Fundamentação: BFS, DFS, Gulosa, A*, Hill Climbing (ideia + quando é ótimo / limitações).
[X] Metodologia: tamanhos de tabuleiro (2,3,5,7,10…), como mediu tempo/memória/expansões, seeds ou protocolo reprodutível.
[X] Resultados: tabelas e/ou gráficos com números reais (substituir placeholders após rodar os experimentos).
[X] Discussão: trade-offs, escalabilidade, por que algum algoritmo falhou ou estourou limite.
[X] Conclusão alinhada aos resultados (sem afirmar o que os dados não mostram).

Conteúdo adicional
[X] Mencionar o uso de indices lineares no código
[X] Explicar distância de Hamming

Bibliografia e citações
[X] Todas as citações no texto têm entrada correspondente no .bib.
[X] Abrir no navegador os links anotados no referencias.bib e conferir se abrem (rede local/firewall podem variar):
    - https://aima.cs.berkeley.edu/ (site oficial do AIMA / Russell & Norvig)
    - https://doi.org/10.1109/TSSC.1968.300136 (DOI do artigo de Hart–Nilsson–Raphael; deve redirecionar para a página do artigo)
[X] Rodar BibTeX + duas passagens de pdfLaTeX (ou o script build.ps1) e verificar se não há “?” nas referências.

Formatação e entrega
[X] Contagem de páginas entre 2 e 6 (conforme professor).
[X] Figuras com legenda e referência no texto (\ref{fig:...}).
[X] Tabelas com legenda e referência no texto (\ref{tab:...}).
[X] Ortografia e consistência de termos (português; siglas explicadas na primeira vez).
[X] PDF final nomeado e posicionado conforme pedido: estudo-dirigido-1/relatorio.pdf (gerado a partir desta pasta latex).

Código (consistência com o relatório)
[ ] Versão do código usada na rodada de experimentos anotada (commit ou data).
[ ] Métricas reportadas batem com a definição no código (ex.: expansões vs tempo).

Questionário sobre o código (responder por escrito para “pegar” o que foi implementado)
[ ] Como um estado do tabuleiro NxN é representado em memória (tipo e significado dos bits)?
[ ] O que exatamente `apply_click` faz em termos de operações de bit (XOR, deslocamento)?
[ ] Qual é o estado objetivo (`goal`) neste projeto e como `mismatch_count` se relaciona com ele?
[ ] Por que `LightsOutProblem` existe separado de `search.py` (qual responsabilidade de cada arquivo)?
[ ] O que `SearchLimits.poll` verifica e por que isso importa em tabuleiros grandes?
[ ] Por que `run_suite` cria um `SearchLimits` novo para cada par (tamanho, algoritmo)?
[ ] Em BFS, para que servem `parent` e `action_from_parent`?
[ ] Em A*, o que significa `best_g` e por que comparamos `g` com `best_g.get(state)` ao desempilhar?
[ ] Qual a diferença prática entre a ordenação da gulosa (só `h`) e a do A* (`g + h`) neste código?
[ ] Por que hill climbing pode terminar com `no_solution` mesmo existindo solução para o tabuleiro?
[ ] O que `measure_callable` mede (tempo e memória) e o que essa memória NÃO mede (limitações do tracemalloc)?
[ ] O que cada coluna da tabela final significa (cabecalhos em portugues; ver rodape sobre memoria). Opcoes: `py main.py --format tsv` ou `--format csv` para planilha.
